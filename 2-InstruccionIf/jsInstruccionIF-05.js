
function mostrar()
{

	var distancia
	var tiempo
	var velocidad
	var tiempoTotal


	distancia = txtIdEdad.value;
	distancia = parseFloat(distancia);
	tiempo = prompt("Cuanto tardaste chabon");
	tiempo = parseFloat(tiempo);
	tiempoTotal = parseFloat(tiempoTotal);
	tiempoTotal = (distancia=velocidad*tiempo);
	velocidad = parseFloat(velocidad);

	
	alert("Velocidad Total" + tiempoTotal);








}

/*
con //if
pedir a una persoan que fue de vaciones , la distancia que viajo y el tiempo que tardo, para calcular la velocidad
e //informar:
60 km/hr = muy lento
hata 80km/h= lento
hasta 100= buen ritmo
hasta 120= ahi de la ley
mas = eso no se hace



function mostrar()
{

	var distancia;
	var velocidad;
	var tiempo;
	var velocidadTotal;

	velocidad = txtIdEdad.value;
	velocidad = parseInt(velocidad);
	distancia = prompt("Ingrese la distancia");
	distancia = parseInt(distancia);
	tiempo = prompt("Cuanto tardaste");
	tiempo = parseInt(tiempo);
	velocidadTotal = parseInt(velocidadTotal);

	velocidadTotal = (distancia=velocidad*tiempo);

		alert("velocidad total" + velocidadTotal);

		if (velocidad<90)
			{
				alert("moooy lento");
			}
		if (velocidad<101)
			{
				alert("Buen ritmo");
			}
		if (velocidad<121)
			{
				alert("ahi de la ley");
			}

		else
		{
			alert("eso no se hace");

		}





}

*/

	


